name: Keep backend alive

on:
  schedule:
    # Every 10 minutes, 24/7
    - cron: '*/10 * * * *'
  workflow_dispatch: # allow manual trigger from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health endpoint
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ secrets.VITE_API_URL }}/health" --max-time 60) || STATUS="timeout"
          echo "Response: $STATUS"
          if [ "$STATUS" = "timeout" ]; then
            echo "Warning: backend timed out (cold start?) — ping still sent"
          elif [ "$STATUS" != "200" ]; then
            echo "Warning: backend returned $STATUS"
          fi
